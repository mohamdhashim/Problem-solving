//gets(to take input with line);
//get char to ignore n in input in gets();
//sscanf(takestring to input);
int n;
   cin>>n;
getchar();
char s[5];
 while(gets(s)&&s[0]!='\0'){ sscanf(s,"%d",i,...); ....}
